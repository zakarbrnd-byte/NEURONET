[workspace]
resolver = "2"
members = ["backend"]
default-members = ["backend"]

[workspace.package]
version = "0.0.0"
edition = "2021"
license = "MIT"
authors = ["Project NEURONET Contributors"]
description = "NEURONET — Artificial Life Operating System (ALOS)"
repository = "https://github.com/zakarbrnd-byte/NEURONET"
readme = "README.md"

[workspace.dependencies]
# Dependency versions will be pinned as modules are implemented.
# Keep additions minimal and justified in ARCHITECTURE.md / PRs.
