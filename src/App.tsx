import { MissionControl } from "./pages/MissionControl";

/**
 * Application root.
 * Mission Control is the only main layout — the old long scrolling debug page is gone.
 */
export default function App() {
  return <MissionControl />;
}
