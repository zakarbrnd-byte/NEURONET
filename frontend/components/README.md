# components

Reusable presentational and interactive UI units for Mission Control.

Examples (future): energy bar, activity row, sidebar module item, control cluster.

Prefer composition. Cards are not the default visual language.
