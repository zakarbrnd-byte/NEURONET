# components

Reusable presentational and interactive UI units for Mission Control.

Examples (future): energy bar, activity feed row, module sidebar item, control button group.

Prefer composition over megacomponents. Cards are not the default visual language.
