# frontend/src

Application source root for Mission Control.

Future entry files (`main.tsx`, `App.tsx`) land here when UI implementation begins.
Until then, keep feature code inside the dedicated subfolders.
